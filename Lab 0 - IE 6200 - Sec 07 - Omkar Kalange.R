getwd() #get working directory

x <- 1 #Storing 1 in x
y <- "1" #ztoring character 1 in y

class(x) #used to print class of variable
class(y)

v=c(9,8,7,6,4)

v[3] # displaying third element
seq

a=matrix(c(1,2,3,4),nrow = 2,ncol=2,byrow='TRUE') #creating 2x2 matrix
a

m=c(5,6)
b=rbind(a,m) #used to add a extra row 
b

n=c(7,8,9)
c=cbind(b,n) #used to add an extra column
c

head(iris,5) #first 5 rows
tail(iris,5)#last 5 rows

